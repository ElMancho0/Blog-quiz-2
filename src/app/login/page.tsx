"use client";
import Login from '@/app/components/Login';

export default function Main(){
    return (
        <main className="flex min-h-screen flex-col items-center bg-gradient-radial rounded-lg bg-beige">
        <Login/>
      </main>

    );
}